
//
// Implements the Fibonacci function recursively. The implementation caches the result when it is calculated the first
// time. Caching mechanism allows pruning the Fibonacci recursion tree. Caching approach is possible since Fibonacci
// is a pure function.
//
unsigned int CachedFibonacci(unsigned int num);
